import styled from '@emotion/styled';

const TableDataCell = styled.td`
  padding: 0.5rem;
  text-align: start;

  &:first-of-type {
    padding-left: 0.5rem;
    padding-right: 2rem;
  }
`;

export default TableDataCell;
